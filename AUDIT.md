# 运行时审计（2026-08-19）

## 月相与纵向地图增量

- 玩家重力方向现在统一驱动重力、跳跃、下砸、地面支撑、踩敌判定和碰撞锚点；反转时保留视觉中心并将角色旋转 180 度。
- 相机增加独立纵轴状态，世界层统一应用双轴变换，HUD、剧情气泡和商店保持屏幕空间。
- `AreaRegion.transition` 支持平滑跟随和跨区域边界切换；区域范围同时约束 X/Y 相机。
- 镜像门采用接触触发和双方冷却，避免抵达后立即循环传送；月相开关和商店统一使用可重映射交互键。
- 联动平台按 `group` 配对，并以 `sign` 决定相反运动方向；无人承载时回归中心。
- Boss 支持 1-4 阶段、阈值切换保护、逐阶段速度/跳跃/弹幕强化，并在顶部 HUD 显示阶段。
- 综合测试关扩展到 4096 x 576，新增下层纵向画廊、边界切换区域、两座联动平台、两个月相开关、商店和镜像门。

## 本轮已修复

- 选关入口曾把点击事件误当作标签名，导致两个关卡列表都隐藏；现已归一化为 `story` / `custom`。
- 编辑器已从无效的 Canvas 子 DOM 节点迁移到真正的 Canvas 绘制，默认场景直接显示地面、辉夜、月门和游戏背景。
- 新增声明式 `StoryTrigger`：开场/区域触发、多步骤台词、指定角色或物体锚点、镜头中心与移动时间、自动停留、单次触发和场景冻结。
- 古代月都背景只使用用户提供的原图，以奇偶镜像段连续铺设；移除额外程序建筑、云层和现代城市叠加。
- 修复低顶棚下变大、蹲姿起跳和落地站起可能嵌入方块的问题；空间不足时延迟变大或保持蹲姿。
- 敌人进入死亡下坠后不再伤害玩家或被重复击杀；终结状态与通关状态不再同帧继续结算。
- 设置、暂停、剧情、死亡和通关状态统一阻断玩法输入；返回标题时清理残留遮罩和剧情状态。
- 自定义关卡结算不再跳到剧情 1-1；存档两部分独立解析，地图导入增加文件尺寸、维度、对象数和数值检查。
- 编辑器限制出生点、月门和多格组件的有效边界；删除目标对象时清除剧情引用，并保证试玩前存在地面、出生点和月门。
- 月门绘制改为比例布局，不再因 64px 小尺寸产生负矩形；经典 1-1 月门前移到 `x=6240`。
- 移动平台携带玩家时会检测墙体/顶棚，发生挤压风险则暂停该帧平台位移；随机寿司不再生成在移动平台上。
- CRT 与调试开关集中到设置页；键位绑定可监听下一次按键、处理冲突、恢复默认并持久化保存。
- 编辑器从固定 36 格扩展为 36-512 格，可横向滚动、翻页和跳转首尾；缩短地图前会阻止删除仍有组件的区域。
- 碰撞体增加启用状态与单向模式，玩家、敌人、道具、火球、变身和平台携带分别按正确方向筛选，不再把单向平台当四向墙。
- 新增成对月井传送、横向 `AreaRegion` 相机边界、淡出/定位/淡入过场和出口锁，避免同帧跨区碰撞与往返振荡。
- 新增单向与延时坠落平台；坠落平台可关闭碰撞、离场、延迟重生，并在玩家占位时等待安全重生。
- 水/岩浆替换为月尘低重力场与月蚀裂隙：前者支持阻力、浮跃和粒子，后者按声明式伤害/间隔结算。
- 新增 Boss 状态机、远程弹体、受击冷却、顶部大血条和统一伤害飘字；Boss 锁门在全部 Boss 退场前不会结算。
- 新增模拟时间倒计时、TIME UP、冻结场景停表和剩余时间奖励；结算不再把暂停/设置/冻结剧情计入用时。
- 编辑器对象均有稳定 UID 和属性检查器，可配置方块掉落/次数、砖块破坏、平台长度/运动、敌人巡逻、Boss、区域、环境、传送与入场能力；剧情镜头支持坐标、画布点选和对象吸附。

## 后台浏览器验证

- 标题、中文选关和关卡卡片均正常显示，卡片布局盒不再是 `0 x 0`。
- 正式 1-1 能加载，古代月都建筑背景与地面、HUD、角色同时正常渲染，控制台无新错误。
- 编辑器默认角色可见，组件使用游戏内图像/绘制效果，背景连续预览，剧情字段可添加和编辑。
- 从编辑器导出的开场剧情可实际试玩：气泡显示、辉夜锚点、镜头平滑移动、点击推进以及剧情结束后的镜头恢复均正常。
- 键位重绑已验证即时更新与刷新后保留；224 格编辑地图已验证末端绘制与 7168px 世界宽度试玩。
- `node --check game.js`、主地图 JSON 解析和 CSS 大括号检查通过。
- 综合测试关加载为 3072px 世界，识别 2 个传送门、2 个单向平台、2 个坠落平台、低重力场、裂隙和 Boss；后台截图确认外城、记忆回廊、环境场与 Boss HUD 正常绘制。
- 后台交互确认月井从外城传送到记忆回廊；编辑器可放置幸运方块、将掉落改为月牙并把触发次数改为 3，随后成功试玩导出地图。

## 仍需持续回归的风险

- 移动平台已有往复/单向/坠落预设；摆动、联动天平、循环轨道和多个平台互推仍未实现。
- 复杂低顶棚组合、多个敌人同时接触、连续顶多个相邻方块仍应在每次物理修改后回归。
- 自定义角色路径可引用外部图片；坏链接会回退默认辉夜，但公开部署仍应配置 CSP 和可信素材策略。
- 超长社区地图虽有限额，仍需在移动端和低性能设备验证内存、Cookie/localStorage 配额及编辑器横向滚动。
- 完整经典关卡仍缺少龟壳与连击、飞行/投掷/炮台敌人、隐藏多金币块、藤蔓/弹簧、城堡迷宫、复杂升降、垂直相机和世界生命流程，不能据此宣称已完整兼容全部关卡。

当前阶段已达到“可稳定试玩并能制作多区域、特殊环境、Boss 与镜头剧情的横版关卡”，尚未达到全部经典关卡兼容状态。
