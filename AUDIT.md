# 运行时审计（2026-08-19）

## 2026-08-31 目标锁、区域、音频与工坊阶段

- 修复仅指定 `cameraX` 的旧剧情错误使用角色脚底补 `cameraY` 的问题；现在纵轴保持当前镜头。剧情结束增加 0.28 秒明确回正，相机数据集回归确认上层剧情 `cameraY=0.00`。
- `smooth` 区域仍约束所属区域的纵轴镜头；玩家离开所有区域时不再保留陈旧 `activeArea`。区域选择按玩家碰撞箱覆盖面积决定，减少边缘抖动。
- 查明测试关“区域空气墙”来自与区域边缘重叠的隐藏 `Solid #6`，并非 `AreaRegion`。该对象已删除；新增显式 `Barrier` 类型，运行时透明、编辑器/Debug 红框可见。
- 低重力区将 `gravityScale` 与 `jumpScale` 分离，允许分别制作慢速降落和高跳；保留空中浮跃力度与阻力。
- 新增固定 `KeyPickup`、`collectedKeyIds` 与 `defeatedEnemyIds`。三类门共用 `lockEnabled`、指定敌人/BOSS ID、多钥匙 ID 和旧 `requiresBoss` 兼容逻辑；缺失引用 fail-closed。
- 锁门护盾显示暗淡/点亮月牙和 `TARGET n/N`；月井、镜像门、通关月门在调试状态输出统一锁状态。检查点快照钥匙和目标进度。
- 编辑器新增月钥、空气墙、锁目标勾选器和动态组件教程。引用在编辑器中使用稳定 UID，导出时转换为唯一正整数 Tiled ID；删除目标会清理门锁/剧情/传送引用。
- 含物品砖块 Debug 文本不再只限幸运方块，专用场景截图确认砖块显示 `SUSHI x2`。地图校验新增所有可见对象 ID 唯一且为正整数的要求。
- 删除内置/仓库中的 Mario 1-1 改编图，只保留 128 x 18 的原创机制测试图。测试图新增三把月钥、指定 BOSS 锁、锁定月井/镜像门、空气墙和双倍率低重力区。
- 设置页新增背景/角色/怪物/关卡四条持久化音频总线；首页像素音符只控制背景音乐。当前不捆绑音乐文件，通过 `SuperKaguyaAudio` 注册同源音频。
- 首页增加 GitHub 像素入口、较大的免责声明和社区工坊入口。工坊支持配置 API、分类目录、离线演示回退、依赖确认、SHA-256 校验、精确锁文件与本机安装缓存。
- 工坊只接受声明式 JSON 和已知能力，不执行远程 JavaScript。零依赖演示后端、OpenAPI 3.1、完整 API/安全文档和生产后端生成提示词已加入仓库。
- 后台浏览器真实截图发现并修复新增样式中的多余 `}`：该错误曾使编辑器定位规则被浏览器丢弃、面板排到画布下方。修复后桌面编辑器、音量设置、工坊、剧情、砖块标签、锁门和 390px 首页均截图通过，无横向溢出。
- `node --check game.js`、`node --check workshop-server.js`、测试地图 JSON/唯一 ID 校验及 `node tools/workshop-smoke.cjs` 通过；工坊冒烟解析 6 个包和 6 个精确依赖。

### 本阶段仍然保留的风险

- 工坊后端是演示实现：举报仅存内存，上传、鉴权、审核、对象存储和签名需要按 `docs/WORKSHOP_API.md` 的生产方案实现。
- 工坊机制目前只保存/展示声明式内容；只有显式编写的可信引擎适配器才能把新对象类型加入运行时。演示 Moon Key 已有适配器，任意新 JSON 不会自动变成可执行玩法。
- 第三方寿司源图仍不跟踪。任何改名、压缩或客户端加密都是可逆再分发，不能替代许可；克隆版使用程序化回退，公开版需取得许可或换成可再分发素材。
- 目标引用现要求唯一正整数 ID；旧的非规范第三方地图若缺 ID 会被拒绝，需要先由 Tiled 或编辑器重新导出。

## 2026-08-30 运行时与编辑器回归

- 剧情气泡不再因顶部空间判断直接跳到人物下方；后台截图确认气泡底边位于人物锚点上方，文字与尾巴方向正常。
- 关卡入口能力缓存为本轮起点状态，顶部重启后仍保持 `big + fire`；关卡定义与地图属性两种配置均参与计算。
- 月井和镜像门支持显式目标 ID、单向/自动返回；无效 ID 与自指保持断开，多入口可单向汇入同一目标。编辑器和调试模式绘制带箭头的浅色虚线。
- Boss 的 `hit` 状态不再跳过绘制；专用回归场景确认 80/100 HP、伤害飘字和 Boss 本体可同时显示。
- 大辉夜站立/蹲伏碰撞宽度缩为 28px，跳跃为 26px；32px 单格通道截图确认碰撞框留有余量且没有闪现。
- 倒重力时左右走、跳、蹲帧会在旋转前交换；回归场景中 `facing=right` 使用左源帧再旋转，最终视觉朝向与右移一致。
- 默认通关点可删除；编辑器地图以 `autoPortal=false` 禁用运行时后备门，随机寿司生成也已处理无月门地图。
- 区域边界与低重力区改为拖拽矩形覆盖层，可与地面、砖块和实体同坐标共存；编辑器先绘制区域再绘制实体。
- 编辑器支持草稿自动保存、刷新恢复、未保存关闭/刷新提醒、试玩返回、保存到本机自定义列表，以及作者和描述字段。
- 标题、选关、编辑器、试玩和返回流程加入短促像素遮罩；覆盖页面有入场动画，并尊重减少动态效果设置。CRT 层位于所有游戏内页面之上。
- 后台浏览器完成标题/编辑器/自定义列表/传送连线/无月门试玩/草稿恢复/Boss/倒重力/单格通道截图；相关页面控制台无 error 或 warning。

## 月相与纵向地图增量

- 玩家重力方向现在统一驱动重力、跳跃、下砸、地面支撑、踩敌判定和碰撞锚点；反转时保留视觉中心并将角色旋转 180 度。
- 相机增加独立纵轴状态，世界层统一应用双轴变换，HUD、剧情气泡和商店保持屏幕空间。
- `AreaRegion.transition` 支持平滑跟随和跨区域边界切换；区域范围同时约束 X/Y 相机。
- 镜像门采用接触触发和双方冷却，避免抵达后立即循环传送；月相开关和商店统一使用可重映射交互键。
- 联动平台按 `group` 配对，并以 `sign` 决定相反运动方向；无人承载时回归中心。
- Boss 支持 1-4 阶段、阈值切换保护、逐阶段速度/跳跃/弹幕强化，并在顶部 HUD 显示阶段。
- 综合测试关扩展到 4096 x 576，新增下层纵向画廊、边界切换区域、两座联动平台、两个月相开关、商店和镜像门。

## 本轮已修复

- 选关入口曾把点击事件误当作标签名，导致两个关卡列表都隐藏；现已归一化为 `story` / `custom`。
- 编辑器已从无效的 Canvas 子 DOM 节点迁移到真正的 Canvas 绘制，默认场景直接显示地面、辉夜、月门和游戏背景。
- 新增声明式 `StoryTrigger`：开场/区域触发、多步骤台词、指定角色或物体锚点、镜头中心与移动时间、自动停留、单次触发和场景冻结。
- 古代月都背景只使用用户提供的原图，以奇偶镜像段连续铺设；移除额外程序建筑、云层和现代城市叠加。
- 修复低顶棚下变大、蹲姿起跳和落地站起可能嵌入方块的问题；空间不足时延迟变大或保持蹲姿。
- 敌人进入死亡下坠后不再伤害玩家或被重复击杀；终结状态与通关状态不再同帧继续结算。
- 设置、暂停、剧情、死亡和通关状态统一阻断玩法输入；返回标题时清理残留遮罩和剧情状态。
- 自定义关卡结算不再跳到剧情 1-1；存档两部分独立解析，地图导入增加文件尺寸、维度、对象数和数值检查。
- 编辑器限制出生点和多格组件的有效边界；删除目标对象时清除剧情与传送引用，并保证试玩前存在地面和出生点。月门允许主动删除。
- 月门绘制改为比例布局，不再因 64px 小尺寸产生负矩形；经典 1-1 月门前移到 `x=6240`。
- 移动平台携带玩家时会检测墙体/顶棚，发生挤压风险则暂停该帧平台位移；随机寿司不再生成在移动平台上。
- CRT 与调试开关集中到设置页；键位绑定可监听下一次按键、处理冲突、恢复默认并持久化保存。
- 编辑器从固定 36 格扩展为 36-512 格，可横向滚动、翻页和跳转首尾；缩短地图前会阻止删除仍有组件的区域。
- 碰撞体增加启用状态与单向模式，玩家、敌人、道具、火球、变身和平台携带分别按正确方向筛选，不再把单向平台当四向墙。
- 新增成对月井传送、横向 `AreaRegion` 相机边界、淡出/定位/淡入过场和出口锁，避免同帧跨区碰撞与往返振荡。
- 新增单向与延时坠落平台；坠落平台可关闭碰撞、离场、延迟重生，并在玩家占位时等待安全重生。
- 水/岩浆替换为月尘低重力场与月蚀裂隙：前者支持阻力、浮跃和粒子，后者按声明式伤害/间隔结算。
- 新增 Boss 状态机、远程弹体、受击冷却、顶部大血条和统一伤害飘字；Boss 锁门在全部 Boss 退场前不会结算。
- 新增模拟时间倒计时、TIME UP、冻结场景停表和剩余时间奖励；结算不再把暂停/设置/冻结剧情计入用时。
- 编辑器对象均有稳定 UID 和属性检查器，可配置方块掉落/次数、砖块破坏、平台长度/运动、敌人巡逻、Boss、区域、环境、传送与入场能力；剧情镜头支持坐标、画布点选和对象吸附。

## 后台浏览器验证

- 标题、中文选关和关卡卡片均正常显示，卡片布局盒不再是 `0 x 0`。
- 正式 1-1 能加载，古代月都建筑背景与地面、HUD、角色同时正常渲染，控制台无新错误。
- 编辑器默认角色可见，组件使用游戏内图像/绘制效果，背景连续预览，剧情字段可添加和编辑。
- 从编辑器导出的开场剧情可实际试玩：气泡显示、辉夜锚点、镜头平滑移动、点击推进以及剧情结束后的镜头恢复均正常。
- 键位重绑已验证即时更新与刷新后保留；224 格编辑地图已验证末端绘制与 7168px 世界宽度试玩。
- `node --check game.js`、主地图 JSON 解析和 CSS 大括号检查通过。
- 综合测试关加载为 3072px 世界，识别 2 个传送门、2 个单向平台、2 个坠落平台、低重力场、裂隙和 Boss；后台截图确认外城、记忆回廊、环境场与 Boss HUD 正常绘制。
- 后台交互确认月井从外城传送到记忆回廊；编辑器可放置幸运方块、将掉落改为月牙并把触发次数改为 3，随后成功试玩导出地图。

## 仍需持续回归的风险

- 移动平台已有往复/单向/坠落预设；摆动、联动天平、循环轨道和多个平台互推仍未实现。
- 复杂低顶棚组合、多个敌人同时接触、连续顶多个相邻方块仍应在每次物理修改后回归。
- 自定义角色路径可引用外部图片；坏链接会回退默认辉夜，但公开部署仍应配置 CSP 和可信素材策略。
- 超长社区地图虽有限额，仍需在移动端和低性能设备验证内存、Cookie/localStorage 配额及编辑器横向滚动。
- 完整经典关卡仍缺少龟壳与连击、飞行/投掷/炮台敌人、隐藏多金币块、藤蔓/弹簧、城堡迷宫、复杂升降、垂直相机和世界生命流程，不能据此宣称已完整兼容全部关卡。

当前阶段已达到“可稳定试玩并能制作多区域、特殊环境、Boss 与镜头剧情的横版关卡”，尚未达到全部经典关卡兼容状态。
